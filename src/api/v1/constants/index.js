'use strict'

const HEADER = {
    API_KEY:'x-api-key',
    CLIENT_ID: 'x-client-id',
    AUTHORIZATION: 'authorization'
}

const ROLE_USER = {
    USER: 'USER',
    ADMIN: 'ADMIN'
}

module.exports = {
    HEADER,
    ROLE_USER,
}